{
  "name": "mai",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build --minify terser --assetsDir a",
    "optimize": "tsc && vite optimize",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  },
  "dependencies": {
    "terser": "^5.32.0"
  }
}
